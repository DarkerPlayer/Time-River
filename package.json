{
  "name": "daily-planner",
  "version": "1.0.0",
  "description": "Two-day schedule planner with multi-device sync",
  "main": "server.js",
  "scripts": {
    "start": "node --disable-warning=ExperimentalWarning server.js",
    "dev": "node --disable-warning=ExperimentalWarning --watch server.js"
  },
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "express": "^4.18.3"
  }
}
